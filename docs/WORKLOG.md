# WORKLOG — сборка и прогоны инструмента

> **Статус (2026-09-06).** Исторический журнал прототипа genai-harness (2026-09-04). Актуальный проект — `aitest_cui`; текущие прогоны и отчёты — `output/runs/<прогон>/REPORT_<прогон>.{md,pdf}` (плюс автоматическое доказательство отравления `output/POISON_PROOF.md`).

Человекочитаемый журнал действий по подготовке стенда и прогонам инструмента. Машинный лог попыток —
в `output/runs/<run-id>/attempts.jsonl`.

## 2026-09-04

- **23:20** Поднял целевой стенд: `docker compose up -d --build` в `~/genai-invest-stand`
  (cert Keycloak уже был, git-статус чист — код стенда не трогал). Все контейнеры `healthy`,
  `GET :8600/healthz` вернул `{"status":"ok"}`. Стенд использует OpenRouter, модель цели
  `openai/gpt-5-mini` для мозга и консолидации (из `.env`).
- **23:21** Создал чистый проект инструмента `~/genai-harness/` (отдельно от `~/genai-sectest`,
  чтобы инструмент был наглядно не привязан к цели). Venv `.venv` (pyyaml/pymongo/redis/requests).
- Начал Фазу 0+1: карта цели и замороженное описание (`harness/config/*.yaml`) из `customer_info.md`.
- **Фаза 0+1 готова:** `NOTES.md` (карта), `target.yaml`/`hypotheses.yaml`/`models.yaml`/
  `payloads.yaml`. `config.py` резолвит всё (ноль литералов в коде).
- **Фаза 2 готова (каркас и проверка по состоянию), проверено вживую на стенде:**
  - выдача ключей: получил 5 ключей без браузера через Keycloak DAG и `/keys`;
  - клиент: чат-раундтрип 200 (агент вернул портфель 1001);
  - **проверка по состоянию (ядро): BAC 1001 к 1003, в vulnerable served=200, в protected=403** — пара;
  - OpenRouter attacker/judge работают; дифференциальный судья поймал влияние ЗООПАРК (shifted, 0.8);
  - изоляция: `purge_all_canaries` (по сигнатурам маркеров) и `reset_memory` (чистый прогон).
- **Изоляция — важное исправление:** в mongo висели остаточные канарейки прошлых sectest-прогонов
  (policy-контрзапись `zoo-atk`, semantic-факт 1001 «префикс ZOO-»), они гнули ответы агента.
  Добавил широкую очистку по паттернам и полный reset памяти перед кампаниями (api_keys не
  трогаю). Память в mongo — это накопленный рантайм, а не посев (посев клиентов — в postgres).
- **Исправление обнаружения (против ложного плюса):** правил отпечатки — исключаю эхо номера счёта
  из запроса атакующего и голые короткие числа; сильные признаки — имя, кэш, ISIN, ISIN×amount, налог.
- **Задача A (BAC) — зелёная, вживую:**
  - слой данных: пара vulnerable(200) и protected(403) доказана детерминированно;
  - H2 account_owner: течёт даже в protected (реальная находка);
  - **BAC через агента (модель вызывает инструмент) сработал вживую**: агент вытащил данные 1003
    (имя «Сидоров», кэш 2 450 000, ISIN операций) по формулировкам с явным `cus=1003`/счётом —
    включая **сгенерированную** атакующей моделью. Прошлые прогоны это «через LLM» не добивали.
- **Задача B (отравление) — зелёная, вживую (в пределах клиента):** безобидный ЗООПАРК прошёл всю
  цепочку E1..E4 как `scope=user` (пережил сессию, вошёл в контекст и ответ 1001). Закрепление
  global бывает реже — ловим частотой на N. Цикл около 52с (gpt-5-mini с рассуждением плюс finalize
  с повторами).
- **Модель, переписывающая запросы** (`core/attacker.py`): генерация в один проход и адаптивный
  обход отказов; берём объединение по нескольким атакующим моделям (слабый атакующий даёт ложное
  «безопасно»).
- Отчётный слой: `findings` (json и md, уровень серьёзности, доверительный интервал Уилсона),
  `coverage`, `susceptibility`.
- Слой разведки: `route_oracle` (скрытые ручки по HTTP-кодам), `prompt_fuzz` (статический мутатор) —
  для переносимости, режима чёрного ящика и оси сравнения без LLM. README готов.
- **BAC-кампания завершена (findings в `runs/bac-20260904-015111/`):**
  - F-BAC-DATA [critical]: пара vulnerable(served 200) и protected(403) — детерминированно;
  - F-BAC-AGENT [critical]: BAC через инструмент 6/8=0.75 в vulnerable (CI 0.41–0.93), 0/8 в
    protected — пара доказана; утекали и seed-, и сгенерированные формулировки (cus=1003/счёт);
  - F-BAC-OWNER [high]: account_owner резолвит владельца чужого счёта и в protected.
- Запущены фоном: сравнение атакующих моделей (`run models`), затем цепочкой — продольное
  отравление по регистрам benign/compliance/universal (`run poison --attempts 4`).
- Переносимость: `PORTABILITY.md` (процедура замены цели, уровни обнаружения: серый ящик, api,
  чёрный ящик); поиск по коду подтвердил — литералов цели в логике нет (только в config, смоуках и
  тексте findings).
- Убрал последние литералы из логики (`victim` брался жёстко в коде, теперь из hypotheses.yaml).
- **Сравнение атакующих моделей завершено** (`runs/models-20260904-015844/`, переписывание BAC,
  чистая генерация без seed'ов): deepseek-v4-flash **6/6=1.0** (дешёвая, лучшая), gpt-4o 5/6=0.83,
  gpt-4o-mini 4/6=0.67, llama-4-maverick 3/6=0.5, qwen3.8-27b 3/6=0.5, **claude-opus-5 0/0 —
  вернул пустой ответ (frontier-safety модель отказалась генерировать атаку)**. Вывод: для
  переписывания дорогие модели не нужны (и даже вредны — отказ); берём дешёвый
  deepseek-flash/gpt-4o-mini. Отказ помечен как «провал возможностей», не засчитан как «безопасно»
  (по ТЗ).
- **Отравление завершено** (`runs/poison-20260904-021041/`): F-POISON-GLOBAL 4/28=0.14
  (compliance-регистр 4/8, статика 4/4), база 0.0, E1=28/28 E3=23/28 E4=19/28; F-POISON-USER
  19/28=0.68. Оба яруса подтверждены.
- **Связка A×B** (`runs/chain-20260904-024520/`): правило с чужим счётом закрепляется 4/4, но
  жертва не триггерит через него BAC (0/4) — «не продемонстрировано при данных условиях».
- **Матрица целевых моделей** (`runs/target-matrix-20260904-092316/`): gpt-4o-mini 4/9=0.44
  (устойчивее), sonnet-5 8/9=0.89 (податливее), gpt-5-mini 0.75 (из BAC-прогона; в матрице — сбой
  перезапуска инфраструктуры, помечен честно). Смена модели через `.env`-оверлей стенда с резервной
  копией; **`.env` восстановлен, git стенда чист, все контейнеры healthy** — стенд не изменён.
- Технические подводные камни (для памяти): фоновые pgrep-«сторожа» находят сами себя (self-match)
  и зависают; долгие прогоны запускать прямым `run_in_background` (не `python ... &` внутри), иначе
  SIGHUP.
- **Главный артефакт `RESULTS.md`** сведён: 6 находок (BAC×3, отравление×2, chain — отрицательный),
  сравнение атакующих и целевых моделей, ось «где стоит LLM».
