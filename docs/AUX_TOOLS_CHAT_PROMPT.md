# Промпт для нового чата (вспомогательные инструменты для атак)

> **Исторический промпт — этап планирования уже пройден.** Это стартовое сообщение для отдельного
> чата на этапе, когда трейсер памяти ещё планировался. Этап завершён: трейсер реализован и
> подключён, а вердикт по памяти теперь складывается из журнала трассировки и повторного вопроса
> (агента спрашивают как другого клиента в новом диалоге). Актуальное — `source/harness/oracle/tracer.py`
> (`TraceAnalyzer`, `ctx.tracer()`), раздел `tracer` в `config/target.yaml`, а также `README.md`
> и `docs/ARCHITECTURE.md`. Ниже — исходный промпт, оставленный как история; как руководство к
> действию («пока только обсуждаем, в код не лезем») его больше не используем.

Скопируй текст ниже как первое сообщение в новом чате.

---

Продолжаем работу над aitest_cui. Это отдельный чат про вспомогательные инструменты для атак.

Шаг 0 — сначала разберись в проекте сам (`/home/dev/aitest_cui`), собери собственное понимание, не
полагайся на одну выжимку. Пройдись по `source/harness/` и прочитай главное:
- `source/harness/attack_vectors/{mem,chain}/vector.py` — модули атак на память (они и станут
  пользователями инструмента);
- `source/harness/tasks/memory_poison.py`, `chain_ab.py` — как на самом деле идут отравление и
  консолидация;
- `source/harness/oracle/memory_observer.py`, `oracle/state.py` — что мы уже умеем читать про
  память;
- `source/harness/core/config.py`, `config/target.yaml` — модель памяти (ярусы, лимиты, redis и
  mongo), триггеры finalize; всё настраивается файлами.
При желании подними стенд и посмотри ярусы: `./.venv/bin/python run.py mem` (и `--marker X`).

Затем подними контекст:
- память: `MEMORY.md`, особенно `aitest-cui-aux-tools`, `aitest-cui-attack-vectors`,
  `aitest-cui-isolated-stand`, `genai-sectest-mempoison-test`, `genai-harness-tool`;
- `/home/dev/aitest_cui/docs/AUX_TOOLS_CONTEXT.md` — стартовый контекст этой темы;
- `/home/dev/aitest_cui/docs/ATTACK_VECTORS.md` — контракт модулей атак.

Тема — набор вспомогательных инструментов для атак. Первый из них трейсер памяти: он прослеживает
путь внедрённой записи по ярусам (рабочая память в redis, затем финализация и консолидация, затем
ярусы политики, семантики, эпизодов, диалогов). Хочу, чтобы модули памяти (`mem`, `chain`)
использовали его, если он есть, — это необязательная зависимость; без него они работают как
сейчас, без сбоя.

Пока мы только обсуждаем и планируем, в код не лезем. Потом я дам наработку трейсера на анализ, и
ты посмотришь, как аккуратно связать его с инструментом: каким будет его контракт, где он висит
на `ctx`, как модуль вызывает его по желанию; всё через настройки и без поломки, если трейсера нет.

Начни так: (1) коротко — что трейсер даёт сверх `oracle/memory_observer.py` и `state.snapshot`;
(2) предложи его контракт (вход и выход) и точку подключения на `ctx`; (3) задай мне открытые
вопросы. Что не ломаем: всё через настройки (ноль литералов, всё берётся из `cfg`), наша
детерминированная проверка по состоянию остаётся главным судьёй, переносимость держится на
`target.yaml`. Режим работы — судья, архитектор, программист, тестировщик: обсудили, сделали
небольшой шаг, прогнали, поправили.

---
