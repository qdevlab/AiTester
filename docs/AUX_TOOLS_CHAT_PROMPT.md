# Промпт для нового чата (вспомогательные тулы для атак)

> **ИСТОРИЧЕСКИЙ промпт (этап планирования пройден).** Это стартовое сообщение для отдельного чата
> на этапе, когда трейсер памяти ещё планировался. Этап завершён: трейсер **реализован и подключён**,
> вердикт по памяти теперь даёт **трейс-файл + вопрос-ответ** (чтение агента как другой клиент в
> новом чате). Актуальное — `source/harness/oracle/tracer.py` (`TraceAnalyzer`, `ctx.tracer()`),
> секция `tracer` в `config/target.yaml`, а также `README.md` / `docs/ARCHITECTURE.md`. Ниже —
> исходный промпт как история; как руководство к действию («пока только обсуждаем, в код не лезем»)
> его больше не использовать.

Скопируй текст ниже как первое сообщение в новом чате.

---

Продолжаем aitest_cui — это ОТДЕЛЬНЫЙ чат про вспомогательные тулы для атак.

Шаг 0 — САМ покопай проект (`/home/dev/aitest_cui`), собери своё понимание, не полагайся только на
выжимку. Пройди по `source/harness/`; прочитай ключевое:
- `source/harness/attack_vectors/{mem,chain}/vector.py` — модули атак памяти (будущие клиенты тула);
- `source/harness/tasks/memory_poison.py`, `chain_ab.py` — как реально идёт отравление/консолидация;
- `source/harness/oracle/memory_observer.py`, `oracle/state.py` — что уже умеем читать про память;
- `source/harness/core/config.py`, `config/target.yaml` — модель памяти (ярусы, лимиты, redis/mongo),
  триггеры finalize; всё config-driven.
При желании подними стенд и глянь ярусы: `./.venv/bin/python run.py mem` (и `--marker X`).

Затем подними контекст:
- память: `MEMORY.md`, особенно `aitest-cui-aux-tools`, `aitest-cui-attack-vectors`,
  `aitest-cui-isolated-stand`, `genai-sectest-mempoison-test`, `genai-harness-tool`;
- `/home/dev/aitest_cui/docs/AUX_TOOLS_CONTEXT.md` — сид-контекст этой темы;
- `/home/dev/aitest_cui/docs/ATTACK_VECTORS.md` — контракт модулей атак.

Тема: набор вспомогательных тулов для атак. Первый — трейсер памяти: проследить путь внедрённого
элемента по ярусам (working redis → finalize/консолидация → policy/semantic/episodic/dialog, E1..E4).
Хочу, чтобы модули памяти (`mem`, `chain`) использовали его ЕСЛИ он есть — опциональная зависимость;
без него работают как сейчас (graceful degrade).

Пока ТОЛЬКО обсуждаем и планируем, в код не лезем. Потом я дам проект/наработку трейсера на анализ —
ты посмотришь, как аккуратно «подружить» его с харнессом: контракт тула, где он висит на `ctx`, как
модуль опционально его зовёт, всё config-driven, без слома при отсутствии.

Начни так: (1) кратко — что трейсер даёт СВЕРХ `oracle/memory_observer.py` и `state.snapshot`;
(2) предложи контракт тула (вход/выход) и точку интеграции на `ctx`; (3) задай мне открытые вопросы.
Инварианты не ломаем: config-driven (ноль литералов, всё через `cfg`), наш детерминированный оракул
состояния остаётся рефери, переносимость через `target.yaml`. Режим — судья-архитектор-программист-
тестировщик: обсудили → инкремент → прогон → правка.

---
