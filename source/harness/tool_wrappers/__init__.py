"""Модуль-обёртки внешних тул отравления/ред-тима (часть комплексного инструмента).

Единый контракт (см. base.ToolWrapper): оркестратор payload-агностичен — зовёт обёртку с
(cfg, out_dir, args). Обёртка: (1) чистит стенд командой из конфига; (2) создаёт подпапку
<out_dir>/<tool>/; (3) БЕЗОПАСНО запускает тулу в ЕЁ venv, наведённую на нашу цель, с output-folder
= подпапка; (4) следит/ждёт (таймаут, не падает); (5) ОПЦИОНАЛЬНО просит сильную LLM свести отчёт по
файлам вывода в НАШ формат. Парсер под каждый формат не пишем — это делает LLM.
"""

from .base import ToolWrapper, WRAPPERS, get_wrapper, available_wrappers

# импорт подмодулей -> они саморегистрируются через @register
from . import garak      # noqa: E402,F401
from . import deepteam   # noqa: E402,F401
from . import llamator   # noqa: E402,F401
